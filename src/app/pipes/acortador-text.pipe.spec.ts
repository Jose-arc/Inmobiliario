import { AcortadorTextPipe } from './acortador-text.pipe';

describe('AcortadorTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AcortadorTextPipe();
    expect(pipe).toBeTruthy();
  });
});
